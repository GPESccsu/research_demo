# research_demo 仓库全面分析总结

## 1. 仓库定位与目标

该仓库核心是一个面向科研工作流的 AI 工具 **SciFlow**（前端主目录为 `my-app`），覆盖选题、文献、阅读摘录、实验设计、写作与自检等场景。整体定位不是“单点功能 demo”，而是“研究全流程助手”。

从结构上看，仓库包含：
- 运行入口与脚本聚合（根目录 `package.json`）。
- 主应用（`my-app`，React + Vite）。
- Python 后端（`my-app/backend`，FastAPI）。
- 一些历史/原型文件（根目录 `.jsx` 文件、`my-app.zip`、`备份` 目录）。

## 2. 技术栈与运行方式

### 2.1 前端
- 框架：React 19。
- 构建：Vite 7。
- 代码规范：ESLint（基础规则 + React hooks/refresh 插件）。
- 运行脚本：`dev / build / preview / lint`。

### 2.2 后端
- 框架：FastAPI。
- 依赖：`fastapi`、`uvicorn`、`httpx`。
- 作用：统一 AI 调用入口，转发到多家模型提供商，并提供 Ollama 模型列表查询。

### 2.3 启动关系
- 根目录脚本通过 `npm --prefix my-app` 转发到前端项目。
- 后端独立运行在 `my-app/backend` 下。
- 典型开发模式是前后端并行启动（文档中也提供 Windows 一键启动 bat）。

## 3. 前端架构分析

### 3.1 架构形态
前端目前呈现“**单体主页面 + 局部模块化拆分**”形态：
- `App.jsx` 仍承担大量模块编排与样式定义，体量较大。
- `pages/`、`components/`、`services/` 已存在，说明项目在向分层演进。

这类形态的优点是迭代快；缺点是 `App.jsx` 维护成本较高、回归风险上升。

### 3.2 数据持久化
采用浏览器本地持久化（IndexedDB + localStorage），优势是：
- 无需后端数据库即可离线保存工作进展。
- 对个人研究场景门槛低。

需要注意的点：
- 数据迁移策略（schema 升级）需要长期维护。
- 大体量文献与草稿数据在浏览器存储上限下存在边界。

### 3.3 AI 调用策略
前端封装了统一 AI 调用层：
- `callAI`：文本调用。
- `callAIJSON`：结构化 JSON 调用。
- `callAIChat`：多轮消息调用。

同时通过 provider 配置支持 Anthropic/OpenAI/Ollama/Groq/Together/OpenRouter/SiliconFlow 等多源模型，扩展性较好。

## 4. 后端架构分析

### 4.1 实际实现
当前后端以 `app.py` 为核心，提供：
- 健康检查。
- 通用单轮调用接口 `/api/ai/call`。
- 通用多轮调用接口 `/api/ai/chat`。
- Ollama 模型查询 `/api/ollama/models`。

实现特点：
- 按 provider 差异处理 `anthropic` 与 `openai-compatible` 协议。
- 对错误做了较清晰的 HTTPException 映射。

### 4.2 Prompt 管理
`prompt_loader.py` 使用“文件 prompt + 内联 prompt”混合机制并带缓存，实用性强，便于保持与前端 prompt 一致。

## 5. 文档与代码一致性评估

仓库存在一定“**文档先行/重构中**”痕迹：
- `docs/architecture.md` 与 `backend/README.md` 中提到 `app/api/routes/tasks.py`、`task_runner.py`、`app/tasks/` 等路径，但当前仓库内并未看到对应文件。
- 实际后端是单文件入口 + 少量服务模块。

这通常意味着：
1) 架构目标已规划但尚未完全落地；或
2) 历史重构后文档未完全回写。

建议尽快统一“当前态文档”和“目标态文档”，避免新贡献者误判系统边界。

## 6. 工程成熟度判断

### 优势
- 功能覆盖完整，场景闭环明显。
- 多 AI provider 抽象清晰，且兼容本地与云端。
- 前后端均有基础文档与可运行脚本。
- Prompt 资产已沉淀为独立文件，利于迭代。

### 风险/短板
- 前端主文件体量大，复杂度集中。
- 存在历史文件/备份目录，仓库信噪比偏低。
- 文档与实现存在偏差。
- 自动化测试与 CI 信息不突出（当前更偏“可运行原型”而非“强工程治理”）。

## 7. 建议的演进路线（按优先级）

1. **文档对齐（最高优先级）**
   - 把“现状架构”与“目标架构”拆分成两个文档节，明确差异与里程碑。
2. **前端继续拆分 App.jsx**
   - 将各业务模块继续下沉到 `pages/components`，减少全局状态耦合。
3. **后端路由分层**
   - 从单文件路由逐步迁移到 `routers/services`，先保持兼容再重构。
4. **测试补齐**
   - 前端补关键服务层单测（AI 请求封装、db 读写）。
   - 后端补 provider 适配与错误处理测试。
5. **仓库清理**
   - 标记或移除过期原型/备份文件，降低维护认知负担。

## 8. 结论

这是一个**产品方向明确、功能完成度较高**的科研 AI 工具仓库，已经具备真实使用价值。当前主要问题不在“有没有功能”，而在“工程化治理与结构清晰度”。

若按“文档对齐 → 结构拆分 → 测试与清理”三步推进，项目可从“强功能原型”平滑进入“可持续协作开发”的成熟阶段。
